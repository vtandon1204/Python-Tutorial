# Tuples: list of values grouped together
# all values in tuple have different meaning (heterogenous)
# whereas in list, all values have same meaning (homogenous)

point = (5,6)
print(point[1])

# tuples are immutable (cant be changed)
